describe('반복 일정 통합 테스트', () => {
  // 1. (필수) 반복 유형 선택 통합 테스트
  describe('반복 유형 선택', () => {
    it('매일 반복 일정을 생성하면 캘린더에 🔄 아이콘과 함께 표시된다', async () => {
      // RED 단계: 테스트가 실패하도록 작성
      expect(true).toBe(false);
    });
  });
});
